exchange:
  env: prod
  mode: paper
  symbol: BTC_USDT_Perp
risk:
  inventory_skew_factor: 0.005 # Reduced from 0.1 (10%) to 0.5%
  max_drawdown_pct: 0.1
  max_position_usd: 1000.0
strategy:
  entry_anchor_mode: true
  trend_strategy: 'adaptive' # Options: 'off' (Pure Grid), 'ma_trend' (Always Skew), 'adaptive' (Auto Detect)
  grid_layers: 5
  name: market_maker
  order_amount: 0.001
  refresh_interval: 3
  spread_pct: 0.0005 # Reduced to 0.05% for better fills
